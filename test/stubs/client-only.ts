// Test-only stand-in for the `client-only` package — same rationale as
// server-only.ts. Not currently imported anywhere in the codebase, but
// aliased defensively so a future import doesn't break the whole suite.
export {};
